package constants

// Version is the SDK version, reported in the User-Agent header.
const Version = "0.1.0"

// APIVersion is the ThousandEyes API version this SDK targets. v7 is the only
// version the SDK supports, and the only one Cisco publishes specifications for.
const APIVersion = "v7"

// Code generated by scripts/openapi/GenerateServices from the ThousandEyes
// OpenAPI specification. DO NOT EDIT.
//
// Source: api.yaml (API version 7.0.103)

package network_dynamic_endpoint_test_results

import (
	"context"
	"encoding/json"
	"fmt"

	"github.com/deploymenttheory/go-sdk-thousandeyes/thousandeyes/client"
	"github.com/deploymenttheory/go-sdk-thousandeyes/thousandeyes/constants"
	"resty.dev/v3"
)

type (
	// NetworkDynamicEndpointTestResults handles communication with the Network Dynamic Endpoint Test Results methods of the
	// ThousandEyes API.
	NetworkDynamicEndpointTestResults struct {
		client client.Client
	}
)

// NewNetworkDynamicEndpointTestResults returns a Network Dynamic Endpoint Test Results service bound to client.
func NewNetworkDynamicEndpointTestResults(client client.Client) *NetworkDynamicEndpointTestResults {
	return &NetworkDynamicEndpointTestResults{client: client}
}

// -----------------------------------------------------------------------------
// ThousandEyes API - Network Dynamic Endpoint Test Results
// -----------------------------------------------------------------------------

// FilterDynamicTestNetworkResults retrieve network dynamic test results.
// URL: POST /endpoint/test-results/dynamic-tests/{testId}/network/filter
// Optional query params: window, startDate, endDate, cursor, expand
//
// Returns network metrics (`loss`, `latency`, `jitter` and `bandwidth`) from
// each endpoint agent, for each `roundId` in the requested window.
//
// Fetches every page. Bound the walk with client.WithMaxPages.
//
// Multi-page behaviour on this endpoint is UNVERIFIED: no _links.next
// has been observed from a /filter endpoint. See client.PostPaginated.
func (s *NetworkDynamicEndpointTestResults) FilterDynamicTestNetworkResults(ctx context.Context, testId string, request *DynamicEndpointTestsDataRoundSearch, opts ...client.RequestOption) (*ResourceNetworkDynamicEndpointTestResults, *resty.Response, error) {
	if testId == "" {
		return nil, nil, fmt.Errorf("testId is required")
	}
	if request == nil {
		return nil, nil, fmt.Errorf("request is required")
	}

	endpoint := fmt.Sprintf("/endpoint/test-results/dynamic-tests/%s/network/filter", testId)

	var result ResourceNetworkDynamicEndpointTestResults

	req := s.client.NewRequest(ctx).
		SetHeader("Accept", constants.Accept).
		SetHeader("Content-Type", constants.ApplicationJSON).
		SetBody(request).
		SetQueryParam("aid", s.client.AccountGroupID())

	// Optional query parameters and per-call overrides.
	for _, opt := range opts {
		opt(req)
	}

	// The collection arrives across pages linked by _links.next. Fetching only
	// the first would silently truncate the result, so every page is merged.
	// Bound the walk with client.WithMaxPages when that is not wanted.
	var items []NetworkDynamicEndpointTestResult
	merge := func(page []byte) error {
		var batch []NetworkDynamicEndpointTestResult
		if err := json.Unmarshal(page, &batch); err != nil {
			return fmt.Errorf("decoding results page: %w", err)
		}
		items = append(items, batch...)
		return nil
	}

	resp, err := req.PostPaginated(endpoint, "results", merge)
	if err != nil {
		return nil, resp, err
	}
	result.Results = items

	return &result, resp, nil
}

// GetDynamicTestPathVisResults retrieve path visualization network dynamic test results.
// URL: GET /endpoint/test-results/dynamic-tests/{testId}/path-vis
// Optional query params: window, startDate, endDate, cursor
//
// Returns a summary of the path visualization data collected from each
// endpoint agent to the destination.
//
// Fetches every page. Bound the walk with client.WithMaxPages.
func (s *NetworkDynamicEndpointTestResults) GetDynamicTestPathVisResults(ctx context.Context, testId string, opts ...client.RequestOption) (*PathVisDynamicEndpointTestResults, *resty.Response, error) {
	if testId == "" {
		return nil, nil, fmt.Errorf("testId is required")
	}

	endpoint := fmt.Sprintf("/endpoint/test-results/dynamic-tests/%s/path-vis", testId)

	var result PathVisDynamicEndpointTestResults

	req := s.client.NewRequest(ctx).
		SetHeader("Accept", constants.Accept).
		SetQueryParam("aid", s.client.AccountGroupID())

	// Optional query parameters and per-call overrides.
	for _, opt := range opts {
		opt(req)
	}

	// The collection arrives across pages linked by _links.next. Fetching only
	// the first would silently truncate the result, so every page is merged.
	// Bound the walk with client.WithMaxPages when that is not wanted.
	var items []PathVisDynamicEndpointTestResult
	merge := func(page []byte) error {
		var batch []PathVisDynamicEndpointTestResult
		if err := json.Unmarshal(page, &batch); err != nil {
			return fmt.Errorf("decoding results page: %w", err)
		}
		items = append(items, batch...)
		return nil
	}

	resp, err := req.GetPaginated(endpoint, "results", merge)
	if err != nil {
		return nil, resp, err
	}
	result.Results = items

	return &result, resp, nil
}

// GetDynamicTestPathVisAgentRoundResults retrieve path visualization network dynamic test results details.
// URL: GET /endpoint/test-results/dynamic-tests/{testId}/path-vis/agent/{agentId}/round/{roundId}
//
// Returns a hop-by-hop summary of the path trace data collected during path
// visualization.
func (s *NetworkDynamicEndpointTestResults) GetDynamicTestPathVisAgentRoundResults(ctx context.Context, testId string, agentId string, roundId string, opts ...client.RequestOption) (*PathVisDetailDynamicEndpointTestResults, *resty.Response, error) {
	if testId == "" {
		return nil, nil, fmt.Errorf("testId is required")
	}
	if agentId == "" {
		return nil, nil, fmt.Errorf("agentId is required")
	}
	if roundId == "" {
		return nil, nil, fmt.Errorf("roundId is required")
	}

	endpoint := fmt.Sprintf("/endpoint/test-results/dynamic-tests/%s/path-vis/agent/%s/round/%s", testId, agentId, roundId)

	var result PathVisDetailDynamicEndpointTestResults

	req := s.client.NewRequest(ctx).
		SetHeader("Accept", constants.Accept).
		SetResult(&result).
		SetQueryParam("aid", s.client.AccountGroupID())

	// Optional query parameters and per-call overrides.
	for _, opt := range opts {
		opt(req)
	}

	resp, err := req.Get(endpoint)
	if err != nil {
		return nil, resp, err
	}

	return &result, resp, nil
}

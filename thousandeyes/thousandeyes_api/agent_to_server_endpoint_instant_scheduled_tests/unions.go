// Code generated by scripts/openapi/GenerateServices from the ThousandEyes
// OpenAPI specification. DO NOT EDIT.
//
// Source: api.yaml (API version 7.0.101)

package agent_to_server_endpoint_instant_scheduled_tests

import (
	"encoding/json"
	"fmt"
)

// EndpointAgentSelectorConfig is a discriminated union from the OpenAPI
// specification. Agents selection object based on agentSelectorType. The
// agentSelectorType property selects which variant is populated.
//
// Build one with a NewEndpointAgentSelectorConfigFrom… constructor, which sets the discriminator
// and the variant together. Setting them by hand risks disagreement, which
// MarshalJSON reports rather than silently dropping.
type EndpointAgentSelectorConfig struct {
	// AgentSelectorType names which variant below is populated.
	AgentSelectorType EndpointAgentSelectorConfigType `json:"agentSelectorType"`

	// AgentLabels is set when agentSelectorType is "agent-labels".
	AgentLabels *EndpointAgentLabelsSelectorConfig `json:"-"`
	// AgentTags is set when agentSelectorType is "agent-tags".
	AgentTags *EndpointAgentTagsSelectorConfig `json:"-"`
	// AllAgents is set when agentSelectorType is "all-agents".
	AllAgents *EndpointAllAgentsSelectorConfig `json:"-"`
	// SpecificAgents is set when agentSelectorType is "specific-agents".
	SpecificAgents *EndpointSpecificAgentsSelectorConfig `json:"-"`

	// Raw is the payload as received. It is always populated, and is the only
	// way to reach a variant added to the API after this code was generated.
	Raw json.RawMessage `json:"-"`
}

// NewEndpointAgentSelectorConfigFromAgentLabels returns a EndpointAgentSelectorConfig holding v.
func NewEndpointAgentSelectorConfigFromAgentLabels(v EndpointAgentLabelsSelectorConfig) EndpointAgentSelectorConfig {
	return EndpointAgentSelectorConfig{
		AgentSelectorType: EndpointAgentSelectorConfigTypeAgentLabels,
		AgentLabels:       &v,
	}
}

// IsAgentLabels reports whether the discriminator selects the
// "agent-labels" variant.
func (u EndpointAgentSelectorConfig) IsAgentLabels() bool {
	return u.AgentSelectorType == EndpointAgentSelectorConfigTypeAgentLabels
}

// GetAgentLabels returns the "agent-labels" variant and whether it
// is populated. A false result with a matching discriminator means the payload
// did not decode into this variant.
func (u EndpointAgentSelectorConfig) GetAgentLabels() (*EndpointAgentLabelsSelectorConfig, bool) {
	if !u.IsAgentLabels() || u.AgentLabels == nil {
		return nil, false
	}
	return u.AgentLabels, true
}

// NewEndpointAgentSelectorConfigFromAgentTags returns a EndpointAgentSelectorConfig holding v.
func NewEndpointAgentSelectorConfigFromAgentTags(v EndpointAgentTagsSelectorConfig) EndpointAgentSelectorConfig {
	return EndpointAgentSelectorConfig{
		AgentSelectorType: EndpointAgentSelectorConfigTypeAgentTags,
		AgentTags:         &v,
	}
}

// IsAgentTags reports whether the discriminator selects the
// "agent-tags" variant.
func (u EndpointAgentSelectorConfig) IsAgentTags() bool {
	return u.AgentSelectorType == EndpointAgentSelectorConfigTypeAgentTags
}

// GetAgentTags returns the "agent-tags" variant and whether it
// is populated. A false result with a matching discriminator means the payload
// did not decode into this variant.
func (u EndpointAgentSelectorConfig) GetAgentTags() (*EndpointAgentTagsSelectorConfig, bool) {
	if !u.IsAgentTags() || u.AgentTags == nil {
		return nil, false
	}
	return u.AgentTags, true
}

// NewEndpointAgentSelectorConfigFromAllAgents returns a EndpointAgentSelectorConfig holding v.
func NewEndpointAgentSelectorConfigFromAllAgents(v EndpointAllAgentsSelectorConfig) EndpointAgentSelectorConfig {
	return EndpointAgentSelectorConfig{
		AgentSelectorType: EndpointAgentSelectorConfigTypeAllAgents,
		AllAgents:         &v,
	}
}

// IsAllAgents reports whether the discriminator selects the
// "all-agents" variant.
func (u EndpointAgentSelectorConfig) IsAllAgents() bool {
	return u.AgentSelectorType == EndpointAgentSelectorConfigTypeAllAgents
}

// GetAllAgents returns the "all-agents" variant and whether it
// is populated. A false result with a matching discriminator means the payload
// did not decode into this variant.
func (u EndpointAgentSelectorConfig) GetAllAgents() (*EndpointAllAgentsSelectorConfig, bool) {
	if !u.IsAllAgents() || u.AllAgents == nil {
		return nil, false
	}
	return u.AllAgents, true
}

// NewEndpointAgentSelectorConfigFromSpecificAgents returns a EndpointAgentSelectorConfig holding v.
func NewEndpointAgentSelectorConfigFromSpecificAgents(v EndpointSpecificAgentsSelectorConfig) EndpointAgentSelectorConfig {
	return EndpointAgentSelectorConfig{
		AgentSelectorType: EndpointAgentSelectorConfigTypeSpecificAgents,
		SpecificAgents:    &v,
	}
}

// IsSpecificAgents reports whether the discriminator selects the
// "specific-agents" variant.
func (u EndpointAgentSelectorConfig) IsSpecificAgents() bool {
	return u.AgentSelectorType == EndpointAgentSelectorConfigTypeSpecificAgents
}

// GetSpecificAgents returns the "specific-agents" variant and whether it
// is populated. A false result with a matching discriminator means the payload
// did not decode into this variant.
func (u EndpointAgentSelectorConfig) GetSpecificAgents() (*EndpointSpecificAgentsSelectorConfig, bool) {
	if !u.IsSpecificAgents() || u.SpecificAgents == nil {
		return nil, false
	}
	return u.SpecificAgents, true
}

// UnmarshalJSON decodes the variant selected by agentSelectorType.
//
// An unrecognised discriminator is not an error. The API adds variants without
// a major version change, and failing the whole response because one field is
// new would be worse than leaving that field unpopulated; Raw still holds it.
func (u *EndpointAgentSelectorConfig) UnmarshalJSON(data []byte) error {
	u.Raw = append(u.Raw[:0], data...)

	var tag struct {
		Value EndpointAgentSelectorConfigType `json:"agentSelectorType"`
	}
	if err := json.Unmarshal(data, &tag); err != nil {
		return fmt.Errorf("reading EndpointAgentSelectorConfig discriminator: %w", err)
	}
	u.AgentSelectorType = tag.Value

	switch tag.Value {
	case EndpointAgentSelectorConfigTypeAgentLabels:
		var v EndpointAgentLabelsSelectorConfig
		if err := json.Unmarshal(data, &v); err != nil {
			return fmt.Errorf("decoding EndpointAgentSelectorConfig variant %q: %w", tag.Value, err)
		}
		u.AgentLabels = &v
	case EndpointAgentSelectorConfigTypeAgentTags:
		var v EndpointAgentTagsSelectorConfig
		if err := json.Unmarshal(data, &v); err != nil {
			return fmt.Errorf("decoding EndpointAgentSelectorConfig variant %q: %w", tag.Value, err)
		}
		u.AgentTags = &v
	case EndpointAgentSelectorConfigTypeAllAgents:
		var v EndpointAllAgentsSelectorConfig
		if err := json.Unmarshal(data, &v); err != nil {
			return fmt.Errorf("decoding EndpointAgentSelectorConfig variant %q: %w", tag.Value, err)
		}
		u.AllAgents = &v
	case EndpointAgentSelectorConfigTypeSpecificAgents:
		var v EndpointSpecificAgentsSelectorConfig
		if err := json.Unmarshal(data, &v); err != nil {
			return fmt.Errorf("decoding EndpointAgentSelectorConfig variant %q: %w", tag.Value, err)
		}
		u.SpecificAgents = &v
	}

	return nil
}

// MarshalJSON encodes the variant the discriminator selects.
//
// A discriminator naming a variant that is not populated is an error rather
// than a silent fallback: emitting Raw or null there would send the server
// something the caller did not ask for.
func (u EndpointAgentSelectorConfig) MarshalJSON() ([]byte, error) {
	switch u.AgentSelectorType {
	case EndpointAgentSelectorConfigTypeAgentLabels:
		if u.AgentLabels == nil {
			return nil, fmt.Errorf(
				"EndpointAgentSelectorConfig: agentSelectorType is %q but AgentLabels is not set",
				u.AgentSelectorType)
		}
		return marshalVariant(u.AgentLabels, "agentSelectorType", string(u.AgentSelectorType))
	case EndpointAgentSelectorConfigTypeAgentTags:
		if u.AgentTags == nil {
			return nil, fmt.Errorf(
				"EndpointAgentSelectorConfig: agentSelectorType is %q but AgentTags is not set",
				u.AgentSelectorType)
		}
		return marshalVariant(u.AgentTags, "agentSelectorType", string(u.AgentSelectorType))
	case EndpointAgentSelectorConfigTypeAllAgents:
		if u.AllAgents == nil {
			return nil, fmt.Errorf(
				"EndpointAgentSelectorConfig: agentSelectorType is %q but AllAgents is not set",
				u.AgentSelectorType)
		}
		return marshalVariant(u.AllAgents, "agentSelectorType", string(u.AgentSelectorType))
	case EndpointAgentSelectorConfigTypeSpecificAgents:
		if u.SpecificAgents == nil {
			return nil, fmt.Errorf(
				"EndpointAgentSelectorConfig: agentSelectorType is %q but SpecificAgents is not set",
				u.AgentSelectorType)
		}
		return marshalVariant(u.SpecificAgents, "agentSelectorType", string(u.AgentSelectorType))
	}

	// The discriminator is one this SDK does not know. Round-trip what arrived
	// rather than rejecting a payload the server considers valid.
	if len(u.Raw) > 0 {
		return u.Raw, nil
	}
	if u.AgentSelectorType == "" {
		return []byte("null"), nil
	}
	return nil, fmt.Errorf(
		"EndpointAgentSelectorConfig: unknown agentSelectorType %q and no raw payload to fall back on",
		u.AgentSelectorType)
}

// marshalVariant encodes a union variant with its discriminator reinstated,
// since the variant types do not carry the tag themselves.
func marshalVariant(variant any, property, value string) ([]byte, error) {
	encoded, err := json.Marshal(variant)
	if err != nil {
		return nil, fmt.Errorf("encoding variant: %w", err)
	}

	fields := map[string]json.RawMessage{}
	if err := json.Unmarshal(encoded, &fields); err != nil {
		// Not an object, so there is nowhere to reinstate the discriminator.
		return encoded, nil
	}

	tag, err := json.Marshal(value)
	if err != nil {
		return nil, fmt.Errorf("encoding discriminator: %w", err)
	}
	fields[property] = tag

	return json.Marshal(fields)
}

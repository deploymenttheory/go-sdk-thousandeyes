// Code generated by scripts/openapi/GenerateServices from the ThousandEyes
// OpenAPI specification. DO NOT EDIT.
//
// Source: api.yaml (API version 7.0.97)

package endpoint_agents

import (
	"context"
	"encoding/json"
	"fmt"

	"github.com/deploymenttheory/go-sdk-thousandeyes/thousandeyes/client"
	"github.com/deploymenttheory/go-sdk-thousandeyes/thousandeyes/constants"
	"resty.dev/v3"
)

type (
	// EndpointAgents handles communication with the Endpoint Agents methods of the
	// ThousandEyes API.
	EndpointAgents struct {
		client client.Client
	}
)

// NewEndpointAgents returns a Endpoint Agents service bound to client.
func NewEndpointAgents(client client.Client) *EndpointAgents {
	return &EndpointAgents{client: client}
}

// -----------------------------------------------------------------------------
// ThousandEyes API - Endpoint Agents
// -----------------------------------------------------------------------------

// GetEndpointAgents list endpoint agents.
// URL: GET /endpoint/agents
// Optional query params: max, cursor, expand, includeDeleted, useAllPermittedAids, agentName, computerName
//
// Retrieves a list of endpoint agents in a given account group.
func (s *EndpointAgents) GetEndpointAgents(ctx context.Context, opts ...client.RequestOption) (*ListEndpointAgentsResponse, *resty.Response, error) {

	endpoint := "/endpoint/agents"

	var result ListEndpointAgentsResponse

	req := s.client.NewRequest(ctx).
		SetHeader("Accept", constants.Accept).
		SetQueryParam("aid", s.client.AccountGroupID())

	// Optional query parameters and per-call overrides.
	for _, opt := range opts {
		opt(req)
	}

	// The collection arrives across pages linked by _links.next. Fetching only
	// the first would silently truncate the result, so every page is merged.
	// Bound the walk with client.WithMaxPages when that is not wanted.
	var items []EndpointAgent
	merge := func(page []byte) error {
		var batch []EndpointAgent
		if err := json.Unmarshal(page, &batch); err != nil {
			return fmt.Errorf("decoding agents page: %w", err)
		}
		items = append(items, batch...)
		return nil
	}

	resp, err := req.GetPaginated(endpoint, "agents", merge)
	if err != nil {
		return nil, resp, err
	}
	result.Agents = items

	return &result, resp, nil
}

// GetEndpointAgentsConnectionString get agent connection string.
// URL: GET /endpoint/agents/connection-string
func (s *EndpointAgents) GetEndpointAgentsConnectionString(ctx context.Context, opts ...client.RequestOption) (*ConnectionString, *resty.Response, error) {

	endpoint := "/endpoint/agents/connection-string"

	var result ConnectionString

	req := s.client.NewRequest(ctx).
		SetHeader("Accept", constants.Accept).
		SetResult(&result).
		SetQueryParam("aid", s.client.AccountGroupID())

	// Optional query parameters and per-call overrides.
	for _, opt := range opts {
		opt(req)
	}

	resp, err := req.Get(endpoint)
	if err != nil {
		return nil, resp, err
	}

	return &result, resp, nil
}

// FilterEndpointAgents filter endpoint agents.
// URL: POST /endpoint/agents/filter
// Optional query params: max, cursor, expand, includeDeleted
//
// Retrieves a list of endpoint agents within the specified account group that
// match the specified filters.
func (s *EndpointAgents) FilterEndpointAgents(ctx context.Context, opts ...client.RequestOption) (*FilterEndpointAgentsResponse, *resty.Response, error) {

	endpoint := "/endpoint/agents/filter"

	var result FilterEndpointAgentsResponse

	req := s.client.NewRequest(ctx).
		SetHeader("Accept", constants.Accept).
		SetQueryParam("aid", s.client.AccountGroupID())

	// Optional query parameters and per-call overrides.
	for _, opt := range opts {
		opt(req)
	}

	// The collection arrives across pages linked by _links.next. Fetching only
	// the first would silently truncate the result, so every page is merged.
	// Bound the walk with client.WithMaxPages when that is not wanted.
	var items []EndpointAgent
	merge := func(page []byte) error {
		var batch []EndpointAgent
		if err := json.Unmarshal(page, &batch); err != nil {
			return fmt.Errorf("decoding agents page: %w", err)
		}
		items = append(items, batch...)
		return nil
	}

	resp, err := req.PostPaginated(endpoint, "agents", merge)
	if err != nil {
		return nil, resp, err
	}
	result.Agents = items

	return &result, resp, nil
}

// DeleteEndpointAgent delete endpoint agent.
// URL: DELETE /endpoint/agents/{agentId}
//
// Deletes the agent with the specified `agent_id`.
func (s *EndpointAgents) DeleteEndpointAgent(ctx context.Context, opts ...client.RequestOption) (*resty.Response, error) {

	endpoint := "/endpoint/agents/{agentId}"

	req := s.client.NewRequest(ctx).
		SetHeader("Accept", constants.Accept).
		SetQueryParam("aid", s.client.AccountGroupID())

	// Optional query parameters and per-call overrides.
	for _, opt := range opts {
		opt(req)
	}

	resp, err := req.Delete(endpoint)
	if err != nil {
		return resp, err
	}

	return resp, nil
}

// GetEndpointAgent retrieve endpoint agent.
// URL: GET /endpoint/agents/{agentId}
// Optional query params: includeDeleted
//
// Retrieves details of an agent with the specified `agent_id`.
func (s *EndpointAgents) GetEndpointAgent(ctx context.Context, opts ...client.RequestOption) (*EndpointAgent, *resty.Response, error) {

	endpoint := "/endpoint/agents/{agentId}"

	var result EndpointAgent

	req := s.client.NewRequest(ctx).
		SetHeader("Accept", constants.Accept).
		SetResult(&result).
		SetQueryParam("aid", s.client.AccountGroupID())

	// Optional query parameters and per-call overrides.
	for _, opt := range opts {
		opt(req)
	}

	resp, err := req.Get(endpoint)
	if err != nil {
		return nil, resp, err
	}

	return &result, resp, nil
}

// UpdateEndpointAgent update endpoint agent.
// URL: PATCH /endpoint/agents/{agentId}
//
// Updates the agent with the specified `agent_id`.
func (s *EndpointAgents) UpdateEndpointAgent(ctx context.Context, request *EndpointAgentUpdate, opts ...client.RequestOption) (*EndpointAgent, *resty.Response, error) {
	if request == nil {
		return nil, nil, fmt.Errorf("request is required")
	}

	endpoint := "/endpoint/agents/{agentId}"

	var result EndpointAgent

	req := s.client.NewRequest(ctx).
		SetHeader("Accept", constants.Accept).
		SetHeader("Content-Type", constants.ApplicationJSON).
		SetBody(request).
		SetResult(&result).
		SetQueryParam("aid", s.client.AccountGroupID())

	// Optional query parameters and per-call overrides.
	for _, opt := range opts {
		opt(req)
	}

	resp, err := req.Patch(endpoint)
	if err != nil {
		return nil, resp, err
	}

	return &result, resp, nil
}

// DisableEndpointAgent disable endpoint agent.
// URL: POST /endpoint/agents/{agentId}/disable
//
// Disables an endpoint agent.
func (s *EndpointAgents) DisableEndpointAgent(ctx context.Context, agentId string, opts ...client.RequestOption) (*EndpointAgent, *resty.Response, error) {
	if agentId == "" {
		return nil, nil, fmt.Errorf("agentId is required")
	}

	endpoint := fmt.Sprintf("/endpoint/agents/%s/disable", agentId)

	var result EndpointAgent

	req := s.client.NewRequest(ctx).
		SetHeader("Accept", constants.Accept).
		SetResult(&result).
		SetQueryParam("aid", s.client.AccountGroupID())

	// Optional query parameters and per-call overrides.
	for _, opt := range opts {
		opt(req)
	}

	resp, err := req.Post(endpoint)
	if err != nil {
		return nil, resp, err
	}

	return &result, resp, nil
}

// EnableEndpointAgent enable endpoint agent.
// URL: POST /endpoint/agents/{agentId}/enable
//
// Enables an endpoint agent.
func (s *EndpointAgents) EnableEndpointAgent(ctx context.Context, agentId string, opts ...client.RequestOption) (*EndpointAgent, *resty.Response, error) {
	if agentId == "" {
		return nil, nil, fmt.Errorf("agentId is required")
	}

	endpoint := fmt.Sprintf("/endpoint/agents/%s/enable", agentId)

	var result EndpointAgent

	req := s.client.NewRequest(ctx).
		SetHeader("Accept", constants.Accept).
		SetResult(&result).
		SetQueryParam("aid", s.client.AccountGroupID())

	// Optional query parameters and per-call overrides.
	for _, opt := range opts {
		opt(req)
	}

	resp, err := req.Post(endpoint)
	if err != nil {
		return nil, resp, err
	}

	return &result, resp, nil
}

// Code generated by scripts/openapi/GenerateServices from the ThousandEyes
// OpenAPI specification. DO NOT EDIT.
//
// Source: api.yaml (API version 7.0.100)

package http_server_endpoint_scheduled_test_results

import (
	"context"
	"encoding/json"
	"fmt"

	"github.com/deploymenttheory/go-sdk-thousandeyes/thousandeyes/client"
	"github.com/deploymenttheory/go-sdk-thousandeyes/thousandeyes/constants"
	"resty.dev/v3"
)

type (
	// HTTPServerEndpointScheduledTestResults handles communication with the HTTP Server Endpoint Scheduled Test Results methods of the
	// ThousandEyes API.
	HTTPServerEndpointScheduledTestResults struct {
		client client.Client
	}
)

// NewHTTPServerEndpointScheduledTestResults returns a HTTP Server Endpoint Scheduled Test Results service bound to client.
func NewHTTPServerEndpointScheduledTestResults(client client.Client) *HTTPServerEndpointScheduledTestResults {
	return &HTTPServerEndpointScheduledTestResults{client: client}
}

// -----------------------------------------------------------------------------
// ThousandEyes API - HTTP Server Endpoint Scheduled Test Results
// -----------------------------------------------------------------------------

// GetMultiTestFilteredHttpServerScheduledTestResults filter HTTP server scheduled test results.
// URL: POST /endpoint/test-results/scheduled-tests/http-server/filter
// Optional query params: window, startDate, endDate, cursor, useAllPermittedAids, expand
//
// Returns component-level (DNS, Connect, Wait and Receive) timing for the load
// of an object over HTTP.
//
// Fetches every page. Bound the walk with client.WithMaxPages.
//
// Multi-page behaviour on this endpoint is UNVERIFIED: no _links.next
// has been observed from a /filter endpoint. See client.PostPaginated.
func (s *HTTPServerEndpointScheduledTestResults) GetMultiTestFilteredHttpServerScheduledTestResults(ctx context.Context, request *HttpEndpointTestsDataRoundsSearch, opts ...client.RequestOption) (*HttpMultiEndpointTestResults, *resty.Response, error) {
	if request == nil {
		return nil, nil, fmt.Errorf("request is required")
	}

	endpoint := "/endpoint/test-results/scheduled-tests/http-server/filter"

	var result HttpMultiEndpointTestResults

	req := s.client.NewRequest(ctx).
		SetHeader("Accept", constants.Accept).
		SetHeader("Content-Type", constants.ApplicationJSON).
		SetBody(request).
		SetQueryParam("aid", s.client.AccountGroupID())

	// Optional query parameters and per-call overrides.
	for _, opt := range opts {
		opt(req)
	}

	// The collection arrives across pages linked by _links.next. Fetching only
	// the first would silently truncate the result, so every page is merged.
	// Bound the walk with client.WithMaxPages when that is not wanted.
	var items []HttpEndpointTestResult
	merge := func(page []byte) error {
		var batch []HttpEndpointTestResult
		if err := json.Unmarshal(page, &batch); err != nil {
			return fmt.Errorf("decoding results page: %w", err)
		}
		items = append(items, batch...)
		return nil
	}

	resp, err := req.PostPaginated(endpoint, "results", merge)
	if err != nil {
		return nil, resp, err
	}
	result.Results = items

	return &result, resp, nil
}

// GetHttpServerScheduledTestResults retrieve HTTP server scheduled test results.
// URL: GET /endpoint/test-results/scheduled-tests/{testId}/http-server
// Optional query params: window, startDate, endDate, cursor, expand
//
// Returns component-level (DNS, Connect, Wait and Receive) timing for the load
// of an object over HTTP.
//
// Fetches every page. Bound the walk with client.WithMaxPages.
func (s *HTTPServerEndpointScheduledTestResults) GetHttpServerScheduledTestResults(ctx context.Context, testId string, opts ...client.RequestOption) (*HttpEndpointTestResults, *resty.Response, error) {
	if testId == "" {
		return nil, nil, fmt.Errorf("testId is required")
	}

	endpoint := fmt.Sprintf("/endpoint/test-results/scheduled-tests/%s/http-server", testId)

	var result HttpEndpointTestResults

	req := s.client.NewRequest(ctx).
		SetHeader("Accept", constants.Accept).
		SetQueryParam("aid", s.client.AccountGroupID())

	// Optional query parameters and per-call overrides.
	for _, opt := range opts {
		opt(req)
	}

	// The collection arrives across pages linked by _links.next. Fetching only
	// the first would silently truncate the result, so every page is merged.
	// Bound the walk with client.WithMaxPages when that is not wanted.
	var items []HttpEndpointTestResult
	merge := func(page []byte) error {
		var batch []HttpEndpointTestResult
		if err := json.Unmarshal(page, &batch); err != nil {
			return fmt.Errorf("decoding results page: %w", err)
		}
		items = append(items, batch...)
		return nil
	}

	resp, err := req.GetPaginated(endpoint, "results", merge)
	if err != nil {
		return nil, resp, err
	}
	result.Results = items

	return &result, resp, nil
}

// GetSingleTestFilteredHttpServerScheduledTestResults filter HTTP server result for a scheduled test.
// URL: POST /endpoint/test-results/scheduled-tests/{testId}/http-server/filter
// Optional query params: window, startDate, endDate, cursor, expand
//
// Returns component-level timings for an object load over HTTP.
//
// Fetches every page. Bound the walk with client.WithMaxPages.
//
// Multi-page behaviour on this endpoint is UNVERIFIED: no _links.next
// has been observed from a /filter endpoint. See client.PostPaginated.
func (s *HTTPServerEndpointScheduledTestResults) GetSingleTestFilteredHttpServerScheduledTestResults(ctx context.Context, testId string, request *HttpEndpointTestsDataRoundsSearch, opts ...client.RequestOption) (*HttpMultiEndpointTestResults, *resty.Response, error) {
	if testId == "" {
		return nil, nil, fmt.Errorf("testId is required")
	}
	if request == nil {
		return nil, nil, fmt.Errorf("request is required")
	}

	endpoint := fmt.Sprintf("/endpoint/test-results/scheduled-tests/%s/http-server/filter", testId)

	var result HttpMultiEndpointTestResults

	req := s.client.NewRequest(ctx).
		SetHeader("Accept", constants.Accept).
		SetHeader("Content-Type", constants.ApplicationJSON).
		SetBody(request).
		SetQueryParam("aid", s.client.AccountGroupID())

	// Optional query parameters and per-call overrides.
	for _, opt := range opts {
		opt(req)
	}

	// The collection arrives across pages linked by _links.next. Fetching only
	// the first would silently truncate the result, so every page is merged.
	// Bound the walk with client.WithMaxPages when that is not wanted.
	var items []HttpEndpointTestResult
	merge := func(page []byte) error {
		var batch []HttpEndpointTestResult
		if err := json.Unmarshal(page, &batch); err != nil {
			return fmt.Errorf("decoding results page: %w", err)
		}
		items = append(items, batch...)
		return nil
	}

	resp, err := req.PostPaginated(endpoint, "results", merge)
	if err != nil {
		return nil, resp, err
	}
	result.Results = items

	return &result, resp, nil
}
